# Fixed support closeout plan

Base: 89e494f04d160ddead0f5ea38c1120b0880e251f. No neural training, preprocessing fit, alpha search or new origin months. Preserve the prior fixed_period_review implementation and its historical pass/fail record; the closeout run is a separate, method-specific replacement evaluation path.

R1 uses the already saved mixed-origin Alice forwards against the original saved predictions with exactly rtol=atol=2e-5. Report all-output numerical differences and valid full/active/low target metric impacts separately. Bounds apply only to the identical compared labels and masks. Selected batch probes concern Qcells TCN44, Sanyo TCN42, Sanyo Inverted43/44 and Qcells Inverted42; no repeated parser or CPU search.

R2 accepts each method only if its saved original-origin replay passes the original criterion. No missing-seed mean; accepted single seeds may be reported. Original support is prefix-specific finite nonnegative complete labels; S1 is pointwise nonnegative; S2 is pointwise finite raw. Candidates and input/Daily/Last-value rules match the prior run. New origins are the prefix-specific complement of original eligibility. All comparisons use Daily-finite and valid-origin intersection, separately within each site/support/scope. Diagnostic nonaccepted methods never enter accepted rankings.

R3 uses all origins in 2017-10-01 00:00 through 2017-12-31 23:55, or the fixed 2018-04-01 through 2018-06-30 23:55. Both periods use at least 24 h history and up to 12 h post-origin-end target buffer. January 1 data are target buffer, not additional origin months. Both complete-H144 and pointwise-finite supports derive from the same candidates, fixed finite origin and Daily point intersection. Original 2017 archived counts remain separately reported. Use the existing official 2018 NIST annual archive for January 1 buffer, and the existing Yulara source. Preserve finite negative external labels.

All temporal intervals: origin-date nonoverlapping 48 h blocks primary, 24/72 h sensitivity, 2000 draws, seed 20260908. Share indices across methods/seeds within each support; recompute RMSE from SSE/count. Average seed-specific effects, never ensemble predictions. Separate periods are not date-paired. Add a second bootstrap implementation without calling production paired.intervals. Acceptance count is observed, never fixed to 20 failures.

Implementation clarification before four-cell scores: an initial Yulara Inverted43 mixed-batch replay differed by up to 3.62396e-5 kW and failed the unchanged tolerance, while the archival batching passed. Therefore record that mixed-batch failure and use an explicit canonical inference partition: newly recompute archived origins in their original chronological batches, then forward nonarchived origins separately. All values are newly inferred from the same frozen checkpoint; no saved original predictions are spliced. This preserves historical batching without adjusting parameters or selecting from period scores.
