# Diagnostic revision plan (2026-09-08)

Source: 14942f4; existing known-Test analyses remain post hoc. No neural training or replacement of frozen evidence.

P01 first reconstruct the actual original Ridge designs using saved Train processors, saved standardization and coefficients. Check actual timestamp maps and reproduce Test arrays; compare Cholesky solutions of the same standardized, centered multi-output objective `||Y-XB||² + alpha ||B||²` against saved spectral solutions. Diagnose feature scales, Train/Validation/Test shifts, contributions, prediction distributions and SSE concentration before deciding whether to expand the alpha grid. No clipping. Any later grid expansion must cover five systems and both information sets and be selected by full-H144 Validation MSE only.

P02 candidate origins are every five-minute coordinate within each Qcells split. Overlapping reason flags record history/tail boundary, missing/nonfinite future labels, finite negative future labels, invalid origin power and Daily missingness. Mutually exclusive attribution uses boundary, future nonfinite, future negative, origin invalid, retained (in that order). Daily is a separate point intersection, never silently treated as primary origin exclusion. Compare H12 support, H144 support and their difference; report target-pair and distinct physical timestamp counts separately. Explain training's historical rule separately if it differs from evaluation.

P03 original Ridge A/B (not a newly tuned replacement) compared on the same common-H144 and Daily-finite point support with Daily and all three Inverted seeds. Five systems, full/power-active/low-power, 48 h primary and 24/72 h sensitivity; 2000 resamples, seed 20260908. Blocks use origin dates from Test-start midnight, retain empty blocks, and share each draw between methods and seeds. Compute SSE/count RMSE first; effect = RMSE(B) - RMSE(reference), negative favors B. Mean neural-seed contrasts average seed-specific effects under the same temporal draw. Report per-seed and mean effects without conflating deterministic Ridge with seed repetition. Conditional temporal intervals do not include model refitting, remove all serial dependence, or establish independent-site climate replication.

P04–P06 follow diagnosis, not anticipated Test gains. Rebuild current figures with final-size typography, place new diagnostics before historical Supplement tables, preserve original results and author declarations. Official journal sources and CAS unknowns remain separate from suitability judgment.

Validation includes SHA-256 before/after frozen sources, true matrix residuals and prediction agreement, index/mask identities, exclusion reconciliation, independent block-SSE recomputation, PDF QA and three verified archives. Counts describe checks, not independent experiments. Original full neural retraining/old checkpoint reproduction are not claimed this round.

## P01 decision after actual-matrix diagnosis, before expanded-grid scores

Seven of ten original selections hit 0.1/1000. Yulara Cholesky and spectral predictions agree within 9e-9 kW; rare weather-missing columns have Train scale about 0.007595 and large subsequent frequency shifts. This supports a regularization-sensitivity check, not numerical repair. All ten fits will evaluate `10**k, k=-4,...,8` (13 values), using the same original Train-fitted transformations, objective, full-H144 Validation MSE and larger-alpha tie rule. No feature removal, clipping, new Test filtering or neural changes. Store all 130 Validation scores and ten selected results separately. The original-grid predictions remain the basis of P03 intervals, with any expanded-grid results explicitly supplementary; do not substitute them silently.
