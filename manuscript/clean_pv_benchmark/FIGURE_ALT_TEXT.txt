Figure 1. Three-layer workflow showing the co-located Sanyo, Hanwha, and Qcells arrays; Train-only preprocessing; a 17-dimensional causal L72 history, forecast origin, and H144 target; and shared origins, labels, and masks for neural and persistence evaluation.

Figure 2. Two aligned logarithmic dot plots show the post hoc neural-envelope RMSE divided by Last-value or matched Daily Persistence RMSE for 24 combinations. All points are shown. Daily is lower in 22 comparisons; Hanwha H12 full and daylight are the two exceptions.

Figure 3. Six panels arrange full-timeline and daylight evaluations in rows and Sanyo, Hanwha, and Qcells in columns. Five method curves show Train-range nRMSE from H12 through H144, revealing horizon growth and array- and scope-dependent ranking changes.

Figure 4. Neural-focused scatter plot of macro Train-range nRMSE against logarithmic batch-one GPU latency for four compact implementations. Bubble area is proportional to parameter count, bold borders mark the within-environment Pareto frontier, and a text annotation reports the off-scale Last-value macro error.

Supplementary Figure S1. Heat map of model RMSE ranks for all 24 primary comparisons. Rows identify array, forecast horizon, and evaluation scope; columns identify the four compact neural implementations and Last-value Persistence. Rank patterns vary across conditions.

Table 1. The three co-located Alice Springs PV arrays, documented module technologies and DC ratings, Active Power target, and horizon-specific Test forecast-origin counts used identically by neural forecasts and Last-value Persistence.

Table 2. Identity and scope of the four compact neural implementations, including architectural inspiration, retained mechanism, omitted official components, parameter count, common input-output mapping, and the interpretations permitted by this controlled project comparison.

Table 3. Primary Test Train-range nRMSE at H12 and H144 for each array and scope, comparing the post hoc minimum three-seed mean among four neural implementations with Last-value Persistence on identical targets; the neural value is a descriptive envelope, not a prespecified model.

Table 4. Unified RTX 3060 Laptop GPU measurements for four neural implementations, showing parameters, macro normalized error, batch-one mean latency, batch-256 throughput, and peak allocated memory in MiB under one fixed environment.
