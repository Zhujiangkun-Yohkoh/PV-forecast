Figure 1. Three-layer causal benchmark workflow showing three co-located arrays, Train-only preprocessing, a 17-dimensional L72 history, the forecast origin, and an H144 target. Within each array, every method uses the same forecast origins, labels, and point masks.

Figure 2. Two aligned logarithmic dot plots show the post hoc neural-envelope RMSE divided by Last-value or matched Daily Persistence RMSE for 24 combinations. All points are shown. Daily is lower in 22 comparisons; Hanwha H12 full and daylight are the two exceptions.

Figure 3. Six panels arrange full-timeline and daylight evaluations in rows and Sanyo, Hanwha, and Qcells in columns. Five method curves show Train-range nRMSE from H12 through H144, revealing horizon growth and array- and scope-dependent ranking changes.

Figure 4. Neural-focused scatter plot of macro Train-range nRMSE against logarithmic batch-one GPU latency for four compact implementations. Bubble area is proportional to parameter count, bold borders mark the within-environment Pareto frontier, and a text annotation reports the off-scale Last-value macro error.

Table 1. Recent PV forecasting studies differ in sites, available future weather information, horizons, model scope, persistence references, and uncertainty treatment. The present study isolates causal history-only compact implementations on matched samples across co-located arrays.

Table 2. The Sanyo, Hanwha, and Qcells arrays are co-located but use different module technologies and DC ratings. Horizon-specific Test origin counts are reported separately for each array and shared only among methods evaluated within that array.

Table 3. Four compact neural implementations retain recurrent, inverted-variate, joint-patch, or depthwise-convolution mechanisms while omitting components of the architectures that inspired them. Parameter counts and batch-one latency describe the common 17-channel H144 task.

Table 4. Primary H12 and H144 Train-range nRMSE compares the descriptive post hoc best-of-four neural envelope with Last-value Persistence on identical horizon-specific targets. The envelope is lower in every listed primary comparison but is not a prespecified model.
